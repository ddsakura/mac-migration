# 專案工作規則

## 修改前先確認分支

- 修改任何檔案前（包含程式、測試、文件），先檢查目前分支與工作目錄狀態。
- 不直接在 `main` 或 `master` 修改；先建立並切換到描述本次工作的 `codex/` 分支，再開始修改。
- 若使用者已建立或指定工作分支，直接沿用，不另開分支。
- 同一項工作的後續修正與 review fixes 沿用該工作分支；新工作若與目前分支無關，先建立適當分支。
- 保留既有未提交修改，不得為切換分支而丟棄、覆寫或重設使用者的工作。
- 未經使用者明確要求，不自動 commit、push 或合併分支。

## 備份與還原驗證

- 使用隔離的假 HOME、暫存目錄與命令替身測試。
- 不對使用者真實資料執行備份或還原，不終止真實 App。
- 維持 macOS 內建 Bash 3.2 相容。
